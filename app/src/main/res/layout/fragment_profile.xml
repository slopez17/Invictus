<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.LinearLayoutCompat xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.profile.ProfileFragment"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:padding="30dp">

        <ImageView
            android:id="@+id/invictus_logo"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_centerHorizontal="true"
            android:contentDescription="@string/app_name"
            android:src="@drawable/ic_invictus_color_100dp" />

        <TextView
            android:id="@+id/invictus_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/invictus_logo"
            android:layout_centerHorizontal="true"
            android:layout_marginStart="10dp"
            android:fontFamily="@font/amaranth"
            android:includeFontPadding="false"
            android:text="@string/app_name"
            android:textSize="35sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/invictus_motto"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/invictus_name"
            android:layout_centerHorizontal="true"
            android:layout_marginStart="10dp"
            android:fontFamily="@font/amaranth"
            android:includeFontPadding="false"
            android:text="@string/app_motto"
            android:textSize="14sp" />
    </RelativeLayout>
</androidx.appcompat.widget.LinearLayoutCompat>